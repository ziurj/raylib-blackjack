# GNU Make workspace makefile autogenerated by Premake

ifndef config
  config=debug_x64
endif

ifndef verbose
  SILENT = @
endif

ifeq ($(config),debug_x64)
  raylib_config = debug_x64
  testing_raylib_config = debug_x64
  staticLib_config = debug_x64

else ifeq ($(config),debug_x86)
  raylib_config = debug_x86
  testing_raylib_config = debug_x86
  staticLib_config = debug_x86

else ifeq ($(config),debug_arm64)
  raylib_config = debug_arm64
  testing_raylib_config = debug_arm64
  staticLib_config = debug_arm64

else ifeq ($(config),release_x64)
  raylib_config = release_x64
  testing_raylib_config = release_x64
  staticLib_config = release_x64

else ifeq ($(config),release_x86)
  raylib_config = release_x86
  testing_raylib_config = release_x86
  staticLib_config = release_x86

else ifeq ($(config),release_arm64)
  raylib_config = release_arm64
  testing_raylib_config = release_arm64
  staticLib_config = release_arm64

else
  $(error "invalid configuration $(config)")
endif

PROJECTS := raylib testing_raylib staticLib

.PHONY: all clean help $(PROJECTS) 

all: $(PROJECTS)

raylib:
ifneq (,$(raylib_config))
	@echo "==== Building raylib ($(raylib_config)) ===="
	@${MAKE} --no-print-directory -C raylib-master -f Makefile config=$(raylib_config)
endif

testing_raylib: raylib staticLib
ifneq (,$(testing_raylib_config))
	@echo "==== Building testing_raylib ($(testing_raylib_config)) ===="
	@${MAKE} --no-print-directory -C game -f Makefile config=$(testing_raylib_config)
endif

staticLib:
ifneq (,$(staticLib_config))
	@echo "==== Building staticLib ($(staticLib_config)) ===="
	@${MAKE} --no-print-directory -C staticLib -f Makefile config=$(staticLib_config)
endif

clean:
	@${MAKE} --no-print-directory -C raylib-master -f Makefile clean
	@${MAKE} --no-print-directory -C game -f Makefile clean
	@${MAKE} --no-print-directory -C staticLib -f Makefile clean

help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  debug_x64"
	@echo "  debug_x86"
	@echo "  debug_arm64"
	@echo "  release_x64"
	@echo "  release_x86"
	@echo "  release_arm64"
	@echo ""
	@echo "TARGETS:"
	@echo "   all (default)"
	@echo "   clean"
	@echo "   raylib"
	@echo "   testing_raylib"
	@echo "   staticLib"
	@echo ""
	@echo "For more information, see https://github.com/premake/premake-core/wiki"